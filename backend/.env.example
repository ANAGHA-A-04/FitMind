# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Connection
# Get your MongoDB URI from: https://cloud.mongodb.com
# Replace <username>, <password>, and <cluster> with your actual values
MONGODB_URI=mongodb+srv://fitmindadmin:application@clusterfit.l8gxnd5.mongodb.net/?appName=Clusterfit


# JWT Configuration
# Generate a secure random string for production
# Example: use a UUID or any long random string
JWT_SECRET=fitmind_super_secret_key_2026
JWT_EXPIRE=7d